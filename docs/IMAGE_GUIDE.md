# README 图片插入指南

本文档说明如何在 README.md 中插入图片。

## 📸 图片插入方式

### 方式一：使用相对路径（推荐）

将图片放在项目目录中，使用相对路径引用。

**步骤：**

1. 在项目根目录创建 `docs/images/` 文件夹（或 `images/` 文件夹）
2. 将图片文件放入该文件夹
3. 在 README.md 中使用以下格式：

```markdown
![图片描述](./docs/images/screenshot.png)
```

**示例：**
```markdown
![主界面](./docs/images/main.png)
![对话界面](./docs/images/chat.png)
![Mermaid图表](./docs/images/mermaid.png)
```

### 方式二：使用绝对路径

如果图片在项目根目录的 `images/` 文件夹中：

```markdown
![图片描述](/images/screenshot.png)
```

### 方式三：使用网络图片

直接使用图片的 URL：

```markdown
![图片描述](https://example.com/image.png)
```

**GitHub 示例：**
```markdown
![GitHub Logo](https://github.com/yourusername/yourrepo/raw/main/docs/images/logo.png)
```

### 方式四：使用 GitHub 仓库中的图片

如果图片已提交到 GitHub 仓库：

```markdown
![图片描述](https://raw.githubusercontent.com/用户名/仓库名/分支名/路径/图片.png)
```

## 📁 推荐的文件夹结构

```
chatw/
├── docs/
│   ├── images/          # 文档图片
│   │   ├── main.png
│   │   ├── chat.png
│   │   └── mermaid.png
│   └── IMAGE_GUIDE.md   # 本文件
├── README.md
└── ...
```

## 🎨 图片格式建议

- **格式**：推荐使用 PNG 或 JPG 格式
- **大小**：建议单张图片不超过 1MB
- **尺寸**：建议宽度不超过 1200px，保持合适的宽高比
- **命名**：使用有意义的文件名，如 `main-interface.png`

## 💡 实际使用示例

在 README.md 中，你可以这样使用：

```markdown
## 📸 项目截图

### 主界面
![主界面](./docs/images/main-interface.png)

### 对话功能
![对话界面](./docs/images/chat-interface.png)

### Mermaid 图表
![Mermaid图表示例](./docs/images/mermaid-chart.png)
```

## ⚠️ 注意事项

1. **路径正确性**：确保图片路径相对于 README.md 文件位置正确
2. **文件存在**：确保图片文件已提交到仓库
3. **大小限制**：GitHub 对单个文件有大小限制（建议 < 1MB）
4. **相对路径**：使用相对路径时，路径是相对于 README.md 文件的位置

## 🔗 在 README 中引用

在 README.md 的相应位置，取消注释并修改路径：

```markdown
## 📸 项目截图

<!-- 取消下面的注释，并修改为你的实际图片路径 -->
![主界面](./docs/images/main.png)
![对话界面](./docs/images/chat.png)
![Mermaid图表](./docs/images/mermaid.png)
```

## 📝 快速开始

1. 创建图片文件夹：
   ```bash
   mkdir -p docs/images
   ```

2. 将截图放入文件夹：
   ```bash
   # 将你的截图文件复制到 docs/images/ 目录
   ```

3. 在 README.md 中引用：
   ```markdown
   ![描述](./docs/images/文件名.png)
   ```

4. 提交到仓库：
   ```bash
   git add docs/images/
   git add README.md
   git commit -m "添加项目截图"
   git push
   ```

---

**提示**：如果图片暂时无法添加，可以先使用占位符或注释，后续再补充。

